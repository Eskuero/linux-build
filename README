
# Dependencies

To build on an x86_64 system running Arch Linux you need the following packages:
```
$ pacman -Syu base-devel git
```

You will also need to install from AUR the following packages (for ARM emulation inside the chroot):
- binfmt-qemu-static
- qemu-user-static-bin

# Building
```
$ git clone https://github.com/Danct12/linux-build/
$ cd linux-build
# make
```
